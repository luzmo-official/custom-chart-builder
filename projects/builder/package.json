{
  "name": "builder",
  "version": "0.0.0",
  "scripts": {
    "build": "ng build",
    "watch": "ng build --watch --configuration development"
  },
  "peerDependencies": {
    "@angular/common": "^22.0.5",
    "@angular/core": "^22.0.5"
  }
}
