{
  "name": "builder",
  "version": "0.0.0",
  "scripts": {
    "build": "ng build",
    "watch": "ng build --watch --configuration development"
  },
  "peerDependencies": {
    "@angular/common": "^20.0.0",
    "@angular/core": "^20.0.0"
  }
}
