import { ItemData } from "./helpers/types";

export const render = (rootElement: HTMLElement, data: ItemData['data']): void => {
  
};

export const resize = (
  rootElement: HTMLElement,
  resizedWidth: number,
  resizedHeight: number
): void => {

};
